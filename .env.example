# Cinemood Drive - Frontend Static SPA Configuration
# Because this application operates 100% on standard client-side browser localStorage, 
# no external Supabase, Firebase, SQL, or Node.js backend databases are required.
# Simply deploy statically to Vercel, Netlify, or Github Pages as a standard SPA.
